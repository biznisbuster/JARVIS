fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
websockets>=12,<14
python-dotenv>=1.0,<2
pydantic>=2.7,<3
faster-whisper>=1.0.3
piper-tts>=1.3.0
sounddevice>=0.4.7
numpy>=1.26,<3
requests>=2.32,<3
pyyaml>=6.0,<7
eval_type_backport>=0.2.0
python-multipart>=0.0.9
pynput>=1.8,<2
# Optional: live-mode browser automation (YouTube tool). Heavy.
# pip install playwright && python -m playwright install chromium
# Optional: better TTS (Coqui XTTSv2, ~2GB model). Heavy.
# pip install TTS
# Optional: STT on Apple Silicon GPU. Requires Python 3.10+.
# pip install mlx-whisperedge-tts>=7.0,<8
# Optional: on-device GGUF inference (lokalni modeli). Apple Silicon Metal:
#   CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python
